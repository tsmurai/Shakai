# 中学受験 社会暗記アプリ プロジェクト構想

## 目的
中学受験の社会(地理)の暗記資料(表・図・グラフが載ったプリントの写真やPDF)を、iPhone/iPadで繰り返し学習できるアプリにする。入試で問われやすい事項を中心に、一覧・フラッシュカード・クイズの3modeで反復学習し、正答率や苦手項目を記録する。

## 利用の流れ
1. 資料(写真/PDF)を `materials/` にアップロード
2. Claude Code のセッションで資料を読み取り、`index.html` 内のデータ(RANKINGS / FACTS)に反映
3. GitHub Pages に自動反映(pushしてから1分程度)
4. 各端末のSafariでURLを開き、ホーム画面に追加してアプリのように使う

## 役割分担
| 役割 | 誰 | 場所 | やること |
|---|---|---|---|
| 開発 | Tsune | イギリス | Windows PC + Claude Code |
| 資料追加 | Tomoko | 日本 | ブラウザで写真/PDFをGitHubにドラッグ&ドロップのみ(Git知識不要)。`UPLOAD_INSTRUCTIONS.md`参照 |
| トライアル利用 | Tsune | イギリス | 自分のiPad/iPhone |
| 本番利用 | Takuma(小6) | 日本 | 自分のiPad/iPhone |

アプリ内のユーザー表示名は実名を避けて `Tsu` / `Taku` / `Tomo`(ホーム画面上部で切り替え、進捗はプロフィールごとに分離)。

## 技術構成
- **開発環境**: Windows PC, Claude Code
- **アプリ形式**: 単一の静的HTML(`index.html`)。データ(表・図の暗記事項)もすべてJS内に埋め込み。ビルド不要
- **公開・配布**: GitHub Pages(公開リポジトリ、https://tsmurai.github.io/Shakai/ )。PWA対応(`manifest.json` / `service-worker.js`、ネットワーク優先キャッシュ)でホーム画面に追加してアプリのように起動、オフラインでも使用可
- **資料の反映**: 写真/PDFは `materials/` に保存 → Claude Code セッションで内容を読み取り、手作業でデータに追記(表計算のような機械的変換ができないため、Excel→JSON変換のような自動化パイプラインは組んでいない。**新しい資料を反映するには毎回 Claude Code セッションが必要**。完全自動化は資料が非構造データ[写真・グラフ]で品質チェックが要るため見送り済み)
- **ユーザー管理**: アプリ内でユーザー切り替え(Tsu/Taku/Tomo)。進捗データ(正答率・誤答数など)はユーザーごとに端末のlocalStorageへ分離保存(クラウド同期なし)
- **出題の優先度**: 各項目に tier(`core`=入試で問われやすい・既定で出題／`ref`=参考データ・一覧のみ既定表示)を付与し、暗記の優先順位を制御
- **カテゴリ**: `国土` / `人口` / `産業` / `交通・貿易`。**`CATS`定数1箇所で管理**しているので、増やすときはここに1行足すだけでよい
- **選択肢プール(`pool`)の注意**: 同じ`pool`内の答えは**表記をそろえる**こと。「北海道」と「北海道（81%・十勝平野）」のように前方一致するものが混ざると、選択肢に両方出て正解を判別できなくなる(実際に33問がこの状態で出題不能になっていた)。割合・産地などの補足は答えに含めず`detail`フィールドへ入れる(回答後・カード裏面・一覧でのみ表示される)
- **グラフ表示の注意**: 出題に関わるグラフ(答えが読み取れてしまうもの)は、回答するかフラッシュカードをめくるまで**必ず非表示**にする(過去に部分マスクで消去法により答えが漏れる不具合を複数回起こしたため、「隠す/全部見せる」の二択に統一済み)

## セキュリティ・プライバシー
- リポジトリは公開(Public)。**資料のファイル名や本文に本名・学校名など個人が特定できる情報を含めないこと**。アップロード時は半角英数字のファイル名を推奨(Mac→Windowsでの文字化け回避)
- 進捗データは各端末内のみに保存(サーバー送信なし)

## 今のデータソース
- `materials/image0.jpeg` : 日本の人口と都市(表8〜表12、図4〜図10) → `人口`カテゴリ
- `materials/image1.jpeg` : 日本の国土のデータバンク(表1〜表7、図1〜図3、世界の地理参考) → `国土`カテゴリ
- `materials/databank2-3.pdf` p1-3 (N64-02 第1次産業のデータバンク): 農業・水産業・林業 → `産業`カテゴリに反映済み
- `materials/databank2-3.pdf` p4-6 (N64-03 第2次産業のデータバンク): エネルギー・鉱工業 → `産業`カテゴリに反映済み
- `materials/databank4.pdf` (N64-04 交通・貿易・資源のデータバンク): 交通・訪日外国人・貿易 → `交通・貿易`カテゴリに反映済み
- `materials/databank9-komin.pdf` (N64-09 公民のデータバンク): 国会・内閣・裁判所・財政・地方自治・ジェンダー平等・国際関係 → `公民`カテゴリに反映済み

※ ファイル名は半角英数字にリネームしてから使うこと(Macでアップロードされた日本語ファイル名はUnicode正規化の違いでWindows/git側から開けないことがある)

## 未着手・次のステップ
- [ ] 新しい資料(写真/PDF)が届いたときの取り込み(継続的に発生する想定)
