# 中学受験 社会暗記アプリ プロジェクト構想

## 目的
中学受験の社会(地理)の暗記資料(表・図・グラフが載ったプリントの写真やPDF)を、iPhone/iPadで繰り返し学習できるアプリにする。入試で問われやすい事項を中心に、一覧・フラッシュカード・クイズの3modeで反復学習し、正答率や苦手項目を記録する。

## 利用の流れ
1. 資料(写真/PDF)を `materials/` にアップロード
2. Claude Code のセッションで資料を読み取り、`index.html` 内のデータ(RANKINGS / FACTS)に反映
3. GitHub Pages に自動反映(pushしてから1分程度)
4. 各端末のSafariでURLを開き、ホーム画面に追加してアプリのように使う

## 役割分担
| 役割 | 誰 | 場所 | やること |
|---|---|---|---|
| 開発 | 本人 | イギリス | Windows PC + Claude Code |
| 資料追加 | 妻 | 日本 | ブラウザで写真/PDFをGitHubにドラッグ&ドロップのみ(Git知識不要) |
| トライアル利用 | 本人 | イギリス | 自分のiPad/iPhone(お父さん(テスト)ユーザーとして) |
| 本番利用 | 息子(小6) | 日本 | 自分のiPad/iPhone(むすこユーザーとして) |

## 技術構成
- **開発環境**: Windows PC, Claude Code
- **アプリ形式**: 単一の静的HTML(`index.html`)。データ(表・図の暗記事項)もすべてJS内に埋め込み。ビルド不要
- **公開・配布**: GitHub Pages(公開リポジトリ)。PWA対応(`manifest.json` / `service-worker.js`)でホーム画面に追加してアプリのように起動、オフラインでも使用可
- **資料の反映**: 写真/PDFは `materials/` に保存 → Claude Code セッションで内容を読み取り、手作業でデータに追記(表計算のような機械的変換ができないため、Excel→JSON変換のような自動化パイプラインは組んでいない。**新しい資料を反映するには毎回 Claude Code セッションが必要**)
- **ユーザー管理**: アプリ内でユーザー切り替え(「お父さん(テスト)」「むすこ」)。進捗データ(正答率・誤答数など)はユーザーごとに端末のlocalStorageへ分離保存(クラウド同期なし)
- **出題の優先度**: 各項目に tier(`core`=入試で問われやすい・既定で出題／`ref`=参考データ・一覧のみ既定表示)を付与し、暗記の優先順位を制御

## セキュリティ・プライバシー
- リポジトリは公開(Public)。**資料のファイル名や本文に本名・学校名など個人が特定できる情報を含めないこと**
- 進捗データは各端末内のみに保存(サーバー送信なし)

## 今のデータソース
- `materials/image0.jpeg` : 日本の人口と都市(表8〜表12、図4〜図10)
- `materials/image1.jpeg` : 日本の国土のデータバンク(表1〜表7、図1〜図3、世界の地理参考)

## 未着手・次のステップ
- [ ] GitHub Pages の公開設定・動作確認
- [ ] 狭い画面幅(iPhone SEなど)での表示崩れ(凡例・ナビの文字がはみ出す可能性)の確認と修正
- [ ] 新しい資料(写真/PDF)が届いたときの取り込み(継続的に発生する想定)
